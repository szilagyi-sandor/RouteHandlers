// TODO: CHECK
export const TODO = "";
// import { getCombinations } from "./combinations";

// // The parentPaths order matters here.
// export const createRoutePathObj = <T extends Record<string, string[]>>(
//   layoutPaths: string[][],
//   routePathObj: T
// ): Record<keyof T, string[]> => {
//   const output = {} as Record<keyof T, string[]>;

//   Object.keys(routePathObj).forEach((key: keyof T) => {
//     const fullRoutePath = [...layoutPaths, routePathObj[key]];
//     output[key] = [...getCombinations(fullRoutePath).map((c) => c.join(""))];
//   });

//   return output;
// };
