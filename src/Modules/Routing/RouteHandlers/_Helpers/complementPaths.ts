// TODO: CHECK
export const TODO = "";
// TODO: FIND OUT WHERE AND WHX THIS WAS USED
// import { getCombinations } from "./combinations";

// export const complementPaths = (route: string[], complement: string[]) =>
//   getCombinations([route, complement]).map((c) => c.join(""));
